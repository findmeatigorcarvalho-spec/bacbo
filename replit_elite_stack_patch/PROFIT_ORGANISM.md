# Profit Organism — Living Max-EV System

**Mission:** every good floor ≥ its peak day · max volume/hour · max WR · max G0 · **zero missed winners** · user-friendly dual chat · $5k–$20k+/day when stake × unit edge matches peak density.

This is not one bot script. It is a **living organism**: senses (rooms) → brains (floors) → immune system (truth/merge) → voice (outbox lanes).

---

## Absolute understanding (locked)

1. **Every floor speaks** — Solo/Golden/Platinum *style*, but per floor/camada. Peak gates frozen.  
2. **Many floors same color** = more trust (coalition), not silence.  
3. **Opposite color same window** = lock one (bankroll law).  
4. **Lane split by FIRE template:**  
   - **No countdown-seconds on fire** → Mr_iv4 money coalition + result under (results may show countdown UX).  
   - **Countdown-seconds / intervalo / janela on fire** → **Gunique** own bot/chat + CD result under that fire.  
5. **Result card = truth:** loss on red ⇒ actual blue ⇒ blue opinions right (and vice versa).  
6. **Never lose one winning opportunity** — zero-miss ledger tracks every proposal, block, send, resolve.

---

## Dollar reality (honest + ambitious)

| Lever | What it is | How we max it |
|--|--|--|
| **Unit edge** | WR / G0 density | Peak-locked gates + vault 0-L patterns + result glue |
| **Volume** | Fires/hour & /day | v2 all-floor proposers + early result cards + CD lane |
| **Capture** | % of would-wins that reach chat | Zero-miss ledger; no LIVE bottleneck |
| **Stake** | $ per unit | Operator sets; organism maximizes **units of G0/day** |

Peak references (forensics): May 19 ≈ 3400+ fires @ ~78% · May 10 ≈ 500+ CD G0 · single floors printed ~$1k/hour class on peak.  
**$5k–$20k/day** = hit **multi-floor peak G0 density** × stake. Organism owns density + capture; you own stake.

---

## Organism anatomy

```
                    ┌─────────────────────────────┐
   79 rooms ───────►│ SENSORS (room DNA, streaks, │
   millions posts   │ hour×color, vault patterns) │
                    └──────────────┬──────────────┘
                                   ▼
                    ┌─────────────────────────────┐
   32 floors ──────►│ BRAINS — peak-locked towers │
   each ≥ peak day  │ each PROPOSES fire+result   │
                    └──────────────┬──────────────┘
                                   ▼
              ┌────────────────────┴────────────────────┐
              ▼                                         ▼
   ┌─────────────────────┐                 ┌─────────────────────┐
   │ MONEY LANE (Mr_iv4) │                 │ COUNTDOWN LANE      │
   │ no-timer fire tmpls │                 │ timer/intervalo fire│
   │ floor coalition     │                 │ → Gunique           │
   │ result glued under  │                 │ CD result glued     │
   └──────────┬──────────┘                 └──────────┬──────────┘
              ▼                                         ▼
                    ┌─────────────────────────────┐
                    │ IMMUNE — opposite-color lock │
                    │ strength rank · truth cards  │
                    │ zero-miss ledger             │
                    └──────────────┬──────────────┘
                                   ▼
                    ┌─────────────────────────────┐
                    │ VOICE — one Telethon outbox  │
                    │ two peers · user-friendly    │
                    └─────────────────────────────┘
```

---

## Intelligence already in the body (use all of it)

| Asset | Role |
|--|--|
| `peak_lock_config` + `*_peak` gates | Floor = its peak-day machine |
| `historical_luxury_seed` | Day-one WR/n/G0 never demoted by thin DB |
| `peak_fidelity_ranker` | Who is below peak / silent / strongest |
| `vault_patterns` | Near-perfect kind×color×hour×dow (0–1L) |
| `edge_whitelist_engine` | Force-live / block cells |
| `g0_offset_oracle` | Timing edge for early results |
| `PROFITABLE_CARD_TYPES` | Which templates print $ |
| `room_*` matrices / streaks / sequence | Sensor weights |
| Vany 500k+ msgs | Lane split + card economics |
| `blocked_signals` | Missed winners (capture leaks) |

---

## Zero-miss law (user-friendly)

For every candidate the organism must record:

`sensed → proposed_by_floors[] → lane → merge_decision → sent|held|blocked → resolved → truth`

- **BLOCK that later WINS** = bug / capture leak → alert + replay into shadow miss  
- **Proposed by ≥1 floor, not sent, later WIN** = miss → never acceptable  
- User sees: clean Mr_iv4 money stream + Gunique countdown stream; results always under their fire  

---

## Build sequence (organism awakening)

1. ~~ONE_STACK / single outbox~~  
2. ~~Peak fidelity ranker~~  
3. **Dual-lane router** (template → Mr_iv4 | Gunique)  
4. **Zero-miss ledger**  
5. **v2 floor proposers** (every floor opinion)  
6. **Vault / whitelist / G0-oracle wired into propose score**  
7. **Full historical room-post pattern mine** (promote only n≥30, holdout WR)  
8. **Stake dashboard** — G0/day × unit$ → projected $/day  

Hard blocks forever: `JUN12A/B`, AUTO relay spam, `DO_NOT_BET_PASSED`, thin &lt;60% junk.

---

## Modules

| File | Job |
|--|--|
| `bot/profit_organism.py` | Aggregate all reports → living directives + $/day projection |
| `bot/dual_lane_router.py` | Classify fire template → money vs countdown peer |
| `bot/zero_miss_ledger.py` | Append-only miss/capture ledger |
| `bot/peak_fidelity_ranker.py` | Floor vs peak + strength |
| `PROFIT_MAX_MODEL.md` | Dual-chat economics |
| `LUXURY_GOAL.md` | Goal alignment |
