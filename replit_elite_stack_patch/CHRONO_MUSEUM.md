# Chrono museum — before stacking

## Locked understanding

Do **not** start by stacking/categorizing (WITH_TIMING vs not, Clock A, kinds, floors).

Walk **every signal fire that was ever created**, with its result, in the order they actually appeared: **first → last**.

Only after that parade makes sense do we stack ideas (timing families, floors, lanes).

## Era note

In Vany history, multi-camada / “stack” language first shows around **07 May 2026** (`CAMADAS DO DIA`).  
Pairs before that are `pre_stack=true` (~1343 complete fire→result pairs). Day-one museum #1–5 already posted; continue with #6+.

## Replit

```bash
# next 5 after first5 (#6–#10)
cd /home/runner/workspace && curl -fsSL -H "Cache-Control: no-cache" -o MC.sh "https://raw.githubusercontent.com/findmeatigorcarvalho-spec/bacbo/cursor/add-engine-gate-registry-d5ba/replit_elite_stack_patch/REPLIT_MUSEUM_CHRONO.sh" && bash MC.sh

# then #11–#15
MUSEUM_OFFSET=10 MUSEUM_LIMIT=5 bash MC.sh
```
