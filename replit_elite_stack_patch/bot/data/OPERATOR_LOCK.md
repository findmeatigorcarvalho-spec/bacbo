# OPERATOR LOCK — do not flatten these axes

This file exists so a later pass cannot “forget” the distinctions already
proved from Mr_iv4 archaeology + peak DB + museum catalog. It is not an
impression. It is the saved product facts.

## Three axes (never mix)

1. **SYSTEM** — floor / gate / camada / config / setup / workspace.
   Examples: `JUN19_peak`, `APR20_perfect`, `ELITE_V2`, `MAY19`, `LIVE`.
   Each one proposes with **its own** coalition math, fires **its own**
   signal, and already had **its own** RESULT card for that fire.
   Peak day / peak n / peak WR / peak G0 belong to **this** axis.
   Putting the bundle back together = every system that already ran,
   firing again, plus the RESULT that already informed that fire.

2. **SKIN / FAMILY / TEMPLATE** — the Telegram card body type.
   FIRE, RESULT, OPS, countdown FIRE, gale FIRE, compact hash, color
   banner, G2 unlabeled, forensic timed RESULT, etc.
   **SOLO, GOLDEN, SEQUENCE, PLATINUM, FLASH are labels on a few FIRE
   families only.** They are not systems. They are not chats. They are
   not the whole catalog. Treating “the product” as those names drops
   almost every profitable card that actually posted on Mr_iv4.

3. **CHAT / SHELF** — where a card is delivered.
   `Mr_iv4` = birth chat of the catalog (first prompt ~17 Mar).
   `UNIQUE_g1`…`gN` = live shelves. A shelf is not a skin and not a floor.

G2 ESTUDO lines and the timed `⏰ Pawtucket / Apostou → Saiu` cards are
**their own styles**. They are not Golden, not Solo, not Sequence.
The Apostou→Saiu countdown card is a live FIRE skin: printed Intervalo is
the outcome timer. Production already proved the predicted color
materializes in that exact countdown. Do not treat Saiu as a finished ball.
Many museum fingerprints have **no registry label** (role UNKNOWN).
Those still count. We are looking for every FIRE and RESULT template
that ever posted, labeled or not.

## Where this is saved (so it cannot rely on chat memory)

| Store | What it holds |
|---|---|
| `bot/data/museum_full_catalog.json` | **827** distinct Telegram templates; **770 born on mr_iv4**, 11 on UNIQUE_g1 |
| `bot/data/museum_triage_keep_trash.json` | KEEP **777** / TRASH 50 |
| `bot/data/museum_chrono_all_pairs.json` | FIRE→RESULT pair instances (evidence of timing, not extra types) |
| `bot/data/signal_ledger.json` | Canonical **43 FIRE + 28 RESULT** families to review (71 money families) |
| `bot/data/peak_lock_config.json` + `peak_fidelity_ranker_report.json` | **32 floors**, each with peak_day / peak_n / peak_wr / g0 |
| `bot/chronology_evidence.py` + `observer_confirm.py` | Four clocks + human casino confirm |
| `bot/data/historical_evidence_manifest.json` | Content-addressed inventory of those files |
| **this file** | Axes + operator facts that the JSON catalogs do not say in words |

Museum role split of 827 templates: UNKNOWN **550**, RESULT **209**, FIRE **34**, OPS **25**, ROOM_RELAY 8, ONLINE 1.
KEEP among UNKNOWN: **505**. That unlabeled mass is the “own style / no category” set.

## Coalition (locked)

Each SYSTEM fires by its own math. For one round/color:
- copies of the **same** score = one vote
- **different** scores **add** (1.78 + 1.59 = 3.37)
- then weight by whether that system’s stated fire was **true** against the factual color from its RESULT card (and observer confirm when present)
- opposite colors in one window = LOCK, not a blend
The bundle is all systems + all their FIRE skins + all their RESULT skins, not one chat name.

## What “awaken peak” means

Wake **systems** (floors/gates) that had peak volume / WR / G0 — and also wake **every skin family** those systems already used, including unlabeled Mr_iv4 templates, not only SOLO/GOLDEN/SEQUENCE.

Highest peak_n floors (historical seed): LIVE 18786 @ 81.13% (2026-05-19), MAR19 12057, ELITE_V2 8522, JUN08 5281, JUN10 4249, MAY19 3427, JUN27 3056, JUN20 3003, JUN19 2908, …

## Canonical FIRE families (43) — not “just solo/golden/sequence”

EMERGINDO, CONFIRMED_ENTER, SOLO_ELITE_ENTER, GOLDEN_ENTER, SEQUENCE_ENTER, PLATINUM_ENTER,
GALE_ENTRE_NOVAMENTE, GALE_RETENTATIVA, JANELA_TIMED, ULTRA_TIE, EMPATE_DIRETO_G0, G0_DIRETO,
PREPARE_G1, COMPACT_HASH, SOLO_APOSTAR, SEQUENCE_APOSTAR, GOLDEN_APOSTAR, PLATINUM_APOSTAR,
FLASH_APOSTAR, SEQUENCIA_ENTER_NOW, SINAL_RETIDO_LIBERADO, CD_FIRE_TIMER_BRT_EDT_APOSTAR,
CD_FIRE_QUANTUM_LOCK, CD_FIRE_RUSH_NS_LEFT, GOD_TIER, GOLDEN_ENTRE_AGORA, G0_MISS_ENTRE_G1,
ENTER_NOW_GENERIC, ENTRE_AGORA_GENERIC, APOSTE_AGORA, G0_ENTRADA_DIRETA, DEPTH_G0_ONLY,
INVERTED_CONTRARIO, ORACLE_LOCK, CERTIFIED_ELITE, GRADE_BANNER, HOT_STREAK, DOUBLE_TIE_LOCK,
GALE_1_FACA_AGORA, APOSTAR_EMPATE, NAMED_EDGE_BANNER, G2_RESERVE, FLASH_CONFIRMADO.

## Canonical RESULT families (28)

AUTO_TIE, WIN_TIER, LOSS_TIER, EMPATE, GREEN_LEGACY_G0/G1, TIMED_WINDOW, FLASH_WIN,
GREEN_COMPACT_G0/G1, COMPACT_LOSS/WIN/TIE_HASH, FORENSIC_INTERVALO, BELL_GANHOU, ORACLE_CARD,
COLOR_BANNER (historically huge send count), G0_ACERTOU_PRIMEIRA, GALE_RECOVERED_CHAIN,
EMPATE_DINHEIRO_DEVOLVIDO, EMPATE_PROTECAO, CD_RES_GREEN_G_BRT, CD_RES_RODADAS_TEMPO,
CD_RES_BELL_GANHOU, G2_RESERVE_WIN, AUTO_WIN, AUTO_LOSS, FINAL_LOSS_AFTER_G3.

The timed `🔵… ⏰ Pawtucket Apostou→Saiu` card is its own countdown FIRE
skin (WIN/LOSS tier + timed window + color banner). Printed Intervalo is
the outcome timer. It is not SOLO/GOLDEN/SEQUENCE-the-label, and it is not
a post-hoc report. Awaken it as it already ran.

## What else besides peak day / WR / volume / G0

- Every Mr_iv4 template since first prompt (~17 Mar), including UNKNOWN
- Chrono FIRE→RESULT pairs (timing proof)
- Gale / janela / empate / compact-hash / CD_FIRE / forensic RESULT families
- Room-source lines that are their own style (G2 ESTUDO is one)
- OPS vs FIRE vs RESULT (TRIPLE LOCK is OPS, not a call)
- Observer-confirmed casino color as the factual weight for coalition
- Four clocks (source, DB, Telegram, casino)

Do not describe this as “in anyone’s head.” It is in these files.

## Reality laws (not a second catalog, not a review queue)

Production history is already true. Writing it to JSON does not make it true;
the posting already happened. Do not ask the operator to “confirm” a skin
that already ran.

1. **Already working.** Any FIRE or RESULT template/skin that posted **more
   than 10 of itself** is working unless the operator retired that system.
   Same for a **back-to-back run longer than 24h** or a **peak day above 50
   signals**. PENDING_REVIEW is not a valid state for that history.

2. **Printed seconds = outcome timer.** If the FIRE card said `15 sec`, the
   outcome materialized in **exactly 15 seconds**, every time. Wall-clock
   hour and packer/round-sync “countdown window” do not change that number.
   Do not HOLD the FIRE until remaining ≤ 12s or ≤ 30s. Post the FIRE with
   the printed seconds on the card.

3. **SOLO / GOLDEN / SEQUENCE are skins, not chats.** Live shelves are
   `UNIQUE_g1`…`gN`. Mr_iv4 is the birth chat of the catalog.

4. **Forensic Apostou→Saiu countdown is a FIRE.** The predicted color and
   the printed Intervalo are the call. That color materializes in that
   exact countdown — already proven in production. Do not argue whether
   the card is “before” or “after” the ball. Do not tell the operator
   Saiu is a finished round they should not bet.

## SEQUENCE / forensic family first

The UNIQUE_g1 card `⏰ Pawtucket / Apostou → Saiu / Tipo: SEQUENCE` is the
live FIRE for this family. Printed Intervalo (28.5s on #2725) is the
outcome timer. Outbox posts that skin at FIRE time with Apostou and Saiu
as the predicted color. Do not wait for SQLite outcome and then re-post
the same card as “already happened.” Museum `📊 SEQUENCE SIGNAL — ENTER NOW 📊`
stays in the catalog as a sibling FIRE. Other families are not flattened.

Free-fire floors for this family (each its own stream, peak-day gates):
`LIVE`, `MAR19`, `ELITE_V2`, `ELITE_V2_PEAK`.

A G0 win repeats what the FIRE already said, so that duplicate is dropped.
A miss, tie, or gale recovery is **not** what the FIRE said — that card still
posts. The room never goes silent on a loss.
